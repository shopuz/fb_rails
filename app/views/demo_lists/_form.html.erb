<%= form_for(@demo_list) do |f| %>
  <% if @demo_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@demo_list.errors.count, "error") %> prohibited this demo_list from being saved:</h2>

      <ul>
      <% @demo_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
